# Core
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
xgboost>=1.7.0
shap>=0.42.0
ruptures>=1.1.0

# Deep Learning
# NOTE: Install PyTorch separately BEFORE running `pip install -r requirements.txt`.
# The plain PyPI index only provides CPU-only builds.
# For GPU (CUDA 12.x) — run first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# For CPU-only — run first:
#   pip install torch torchvision torchaudio
# Then proceed with: pip install -r requirements.txt
torch>=2.6.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# HTTP (required by download_data.py)
requests>=2.31.0

# Serialization (required by classifier.py)
joblib>=1.3.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Best-in-class baselines
lightgbm>=4.0.0
catboost>=1.2
tabpfn>=2.0
